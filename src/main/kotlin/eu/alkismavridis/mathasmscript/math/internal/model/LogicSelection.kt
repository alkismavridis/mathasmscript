package eu.alkismavridis.mathasmscript.math.internal.model

class LogicSelection(sideCapacity:Int) {
    val side1 = ExpressionSelection(sideCapacity)
    val side2 = ExpressionSelection(sideCapacity)
}
